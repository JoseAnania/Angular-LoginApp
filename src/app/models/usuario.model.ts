/* Modelo para el manejo de información de Usuarios (que se loguean) */

// definimos la clase
export class UsuarioModel {

    // definimos las propiedades
    email: string;
    password: string;
    nombre: string;
}

